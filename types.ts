export interface ImageData {
  src: string;
  alt: string;
  category?: string;
}

export interface Testimonial {
  text: string;
  author: string;
}

export interface Feature {
  title: string;
  description: string;
  icon: string;
}